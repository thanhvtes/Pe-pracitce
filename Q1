
#include <stdio.h>

int main()
{
    int arr[1000];
    
    for (int i=1; i<=3; i++){
        scanf("%d", &arr[i]);
    }
    
    int count = 0;
    for (int i=1; i<=3; i++){
        if (arr[i]%2==0){
            count++;
        }
    }
    printf ("%d", count);
    return 0;
}
